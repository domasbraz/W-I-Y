# W-I-Y

## How to Use

This was made on Linux OS, therefore it may be required for running the project, Ubuntu should work.

Docker compose needs to be installed.

On Linux run "sudo docker compose up --build" to start the server.
